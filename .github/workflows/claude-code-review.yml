name: Claude Code Review (disabled)

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled state
        run: echo "Disabled in this public fork. Restore only with trusted triggers, least privilege, and immutable action pins."
